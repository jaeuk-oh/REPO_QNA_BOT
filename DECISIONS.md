# 설계 의사결정 기록 (DECISIONS.md)

각 핵심 기술 선택의 이유와 트레이드오프를 기록합니다.
면접에서 "왜 이걸 썼나요?"가 오면 여기서 답이 나옵니다.

---

## 1. RAG vs 파인튜닝

**결정**: RAG (Retrieval-Augmented Generation) 채택

**이유**
코드베이스는 지속적으로 변경됩니다. 파인튜닝은 모델을 코드에 맞게 학습시키는 방식이라, 코드가 바뀔 때마다 재학습이 필요하고 비용이 매우 높습니다.
RAG는 인덱스(벡터 DB)만 갱신하면 되므로 변경 대응 비용이 거의 없습니다.

**트레이드오프**
- RAG는 질문과 코드 청크 사이의 어휘적/의미적 유사도에 의존합니다. 함수 간 호출 관계나 전체 데이터 흐름 같은 구조적 지식은 파악하기 어렵습니다.
- 파인튜닝 모델은 전체 맥락을 학습하므로 이런 질문에 더 강하지만, 비용과 유지보수 부담이 큽니다.

---

## 2. Slack Socket Mode vs HTTP Webhook

**결정**: Socket Mode (WebSocket) 채택

**이유**
HTTP Webhook은 Slack 서버가 우리 서버로 HTTP 요청을 보내는 방식입니다. 이를 위해 공개 URL(또는 ngrok 같은 터널)이 필요합니다.
Socket Mode는 우리 서버가 Slack에 WebSocket 연결을 먼저 맺고 이벤트를 수신합니다. 공개 URL 없이 방화벽 뒤에서도 동작합니다.

**트레이드오프**
- WebSocket 연결을 항상 유지해야 하므로 서버가 항상 실행 중이어야 합니다 (Render 상시 실행 이유).
- HTTP Webhook 방식은 서버리스(Lambda, Cloud Run)에서 실행 가능하지만 Socket Mode는 불가합니다.

---

## 3. ChromaDB (로컬) vs 클라우드 벡터 DB (Pinecone, Weaviate 등)

**결정**: ChromaDB PersistentClient (로컬 파일 저장) 채택

**이유**
사내 코드베이스는 민감한 코드를 포함할 수 있습니다. Pinecone 같은 클라우드 벡터 DB를 쓰면 임베딩 벡터와 원문 청크가 외부 서버로 나갑니다.
ChromaDB 로컬 방식은 데이터가 서버 디스크에만 머물고, 추가 비용도 없으며, 네트워크 레이턴시도 없습니다.

**트레이드오프**
- 단일 서버에 종속됩니다. 서버가 죽으면 DB도 사라집니다 (Render Persistent Disk로 완화).
- 클라우드 DB는 복제, 백업, 멀티 인스턴스 확장이 기본 제공되지만 우리 규모에서는 불필요합니다.

---

## 4. GPT-4o-mini 선택

**결정**: gpt-4o 대신 gpt-4o-mini 사용 (의도 분류 + 답변 생성 모두)

**이유**
RAG 방식은 "전체 코드를 이해해야 하는 복잡한 추론" 대신 "주어진 코드 청크에서 질문에 답하는" 작업입니다. 이 작업에 gpt-4o 수준의 모델은 과잉입니다.
gpt-4o-mini는 gpt-4o 대비 입력 토큰 기준 약 15배 저렴하면서 코드 설명 품질은 충분합니다.

**트레이드오프**
- 복잡한 멀티파일 추론이나 긴 맥락 이해에서는 mini가 부정확할 수 있습니다.
- 정확도가 부족하다면 `config.py`의 `CHAT_MODEL`을 `gpt-4o`로 변경하는 것으로 즉시 전환 가능합니다.

---

## 5. 언어별 청킹 (RecursiveCharacterTextSplitter.from_language)

**결정**: 언어를 감지해 언어별 구분자로 청킹

**이유**
단순 문자 수 기반 분할은 함수 중간에서 자를 수 있습니다. LangChain의 `from_language`는 Python은 `class`, `def` 기준, JS/TS는 `function`, `=>` 기준 등 언어별 의미 단위로 분할을 시도합니다.
코드의 논리 단위가 하나의 청크에 담길 가능성이 높아져 검색 품질이 향상됩니다.

**트레이드오프**
- 여전히 크기 기반 분할이라 긴 함수는 잘립니다. 함수 간 호출 관계는 여전히 다른 청크에 분산될 수 있습니다.
- AST 기반 분할(함수/클래스 단위로 정확히 자르기)이 더 정확하지만 언어별 파서 구현 비용이 큽니다.

---

## 6. 의도 분류 → RAG 2단계 파이프라인

**결정**: 모든 질문에 RAG를 적용하지 않고, 의도 분류 후 필요한 경우에만 검색

**이유**
"안녕"이나 "이 봇 어떻게 써?" 같은 일반 대화에도 임베딩 + 벡터 검색을 돌리면 불필요한 API 비용과 레이턴시가 발생합니다.
의도 분류(gpt-4o-mini, temperature=0, max_tokens=5)는 매우 저렴하고 빠릅니다. 이 분류 결과로 불필요한 검색을 사전 차단합니다.

**트레이드오프**
- 의도 분류 자체도 API 호출 1회를 씁니다. 오분류 시 엉뚱한 파이프라인으로 라우팅될 수 있습니다.
- 현재 오분류 패턴: "대화 내역 저장 방식은?" → `[일반]`으로 잘못 분류되는 케이스 있음 (프롬프트 튜닝으로 개선 가능).

---

## 7. Structured Output (Pydantic) for 코드 답변

**결정**: `[코드]` 인텐트 답변에 `beta.chat.completions.parse` + Pydantic 스키마 강제

**이유**
자유 형식 텍스트 답변은 파일 경로를 환각(hallucination)으로 만들어 내는 경우가 있습니다. Pydantic 스키마로 `code_refs: list[str]`를 강제하면 GPT가 컨텍스트에 실제로 존재하는 경로만 인용하도록 유도됩니다.
또한 패턴/이유/장단점/신뢰도라는 일관된 포맷이 Slack 메시지 가독성을 높입니다.

**트레이드오프**
- Structured Output은 `gpt-4o-mini`와만 안정적으로 동작합니다. 모델 변경 시 파싱 실패 가능성 있음 (fallback 처리 구현됨).
- 스키마가 고정되어 있어 답변 형식을 유연하게 바꾸기 어렵습니다.

---

## 8. 증분 재인덱싱 (git diff 기반)

**결정**: 전체 재인덱싱 대신 변경된 파일만 재임베딩

**이유**
2일마다 전체 레포를 다시 임베딩하면 파일 수에 비례한 API 비용이 발생합니다. `git diff --name-only`로 변경 파일만 추출해 그 파일의 청크만 삭제 후 재삽입합니다.
대부분의 주기에서 변경 파일은 전체의 10% 미만이므로 비용이 크게 절감됩니다.

**트레이드오프**
- Shallow clone이나 force push 등 git 히스토리가 불연속적인 경우 diff가 실패합니다. 이 경우 전체 재인덱싱으로 폴백하도록 구현했습니다.
- 파일 이름이 바뀐 경우 구 파일 청크가 DB에 남을 수 있습니다 (full reindex로만 정리 가능).
